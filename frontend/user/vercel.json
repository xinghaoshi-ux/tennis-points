{
  "buildCommand": "cd .. && pnpm install && pnpm --filter user build",
  "outputDirectory": "dist",
  "installCommand": "npm i -g pnpm",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
